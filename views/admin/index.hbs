<div class="row">
    <div class="col-md-12 col-xl-4">
        <div class="card stat-widget">
            <div class="card-body d-flex align-items-center justify-content-between">
                <h5 class="card-title mb-0">Kategoriyalar</h5>
                <h2>{{category.length}}</h2>
            </div>
        </div>
    </div>
    <div class="col-md-12 col-xl-4">
        <div class="card stat-widget">
            <div class="card-body d-flex align-items-center justify-content-between">
                <h5 class="card-title mb-0">Bo'limlar</h5>
                <h2>{{atribut.length}}</h2>
            </div>
        </div>
    </div>
    <div class="col-md-12 col-xl-4">
        <div class="card stat-widget">
            <div class="card-body d-flex align-items-center justify-content-between">
                <h5 class="card-title mb-0">Kitoblar</h5>
                <h2>{{book.length}}</h2>
            </div>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-md-12 col-lg-12">
        <div class="card table-widget">
            <div class="card-body">
                <h5 class="card-title">Top kitoblar</h5>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th scope="col">Nomi</th>
                                <th scope="col">File</th>
                                {{!-- <th scope="col">Invoice</th> --}}
                                {{!-- <th scope="col">Price</th> --}}
                                {{!-- <th scope="col">Status</th> --}}
                            </tr>
                        </thead>
                        <tbody>
                            {{#each bookTop}}
                                <tr>
                                    <td><img src="{{img}}" width="60px" alt="">{{title}}</td>
                                    <td><a href="/{{img}}" target="_blank" download>download</a></td>
                                    {{!-- <td>$20</td> --}}
                                    {{!-- <td><span class="badge bg-info">Shipped</span></td> --}}
                                </tr>
                            {{/each}}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>